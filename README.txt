lotto 객체 생성
makeRandomLIst-- 6개 뽑기 후 정렬
- 중복확인 --  checkRandomList

- 중복이 있으면 list 에서 제거 후 -- remOverlapNum
- 다시 random add 생성-- plusrandomNum

list 출력
list 저장
while 문 돌려서 return list

printRandomLIst --list 출력
saveRandomList --list 저장


AllRandomList -- 한사람이 뽑은 리스트 저장소


int checkRandomList -- 확인 후 일치 개수 리턴 함수
list<int> makeResultList 각 check 결과 하나당 list<3,4,5,5+,6> ++
resultPrint -- 

<수익률>

int calcIncom -- makeResultList 받아서 가격당 계산 후 return
int calcbenefit -- incom return 값/ 아까 받은 돈 계산(pay)